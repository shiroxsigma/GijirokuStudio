# 複数音声入力の検証

## 自動テスト

```powershell
.venv\Scripts\python.exe -m unittest test_audio_pipeline -v
```

テストは機器を開かず、タイムスタンプ付きPCMと実際のWebRTC AECを使用する。
遅れた同一音の3入力、独立音の同時入力、重複から独立音への切替、
ADC時刻とコールバック時刻のずれ、44.1kHzから48kHzへの連続変換、
無音区間、96kHz入力の帯域制限、停止時の時間軸と役割トラック長を検証する。
AECは30ms遅れの合成エコー、および独立した2つの再生音が20ms/60ms遅れて
回り込む条件を検証する。同時発話の代用は音節状に振幅変調した倍音信号。
人の声・実際の部屋・Bluetoothを含む機器での品質を保証するテストではない。

## 実機確認

1. マイクとスピーカーをすべて選択し、録音のみモードで開始する。
2. 普段使用する会議音声を再生し、黙っている区間、話す区間、同時に話す区間を作る。
3. 同じ音を複数スピーカーへ出す場合と、別々の音を出す場合を確認する。
4. 無音から再開し、途中で音量を変え、30分程度録音して終盤のずれも確認する。
5. 停止後に `audio_main.mp3`、`audio_self.mp3`、`audio_other.mp3` を確認する。
   相手だけの区間が自分側に残りにくいこと、自分の発話・独立した再生音が
   消えないこと、停止直前の声が保存されることを確認する。
6. 高速ASRと従来Whisperでも同様に確認する。

録音中のログにはAECの有効状態、重複を抑えている入力数が表示される。
データ欠落やキュー溢れは終了ログの回数に含まれる。
新しい録音の `metadata.txt` に `AUDIO_PROCESSING=timestamped_per_mic_aec_dedup`
を記録する。既存MP3は変更されない。

実際にスピーカーから増幅する音が鳴る場合は、録音の重複とは別に、
OSのマイクモニターや他アプリの再生経路を確認する。本アプリのキャプチャは
入力専用で、取得したマイク音をスピーカーへ直接再生しない。

## 方式の根拠

- [Microsoft: WASAPI loopback recording](https://learn.microsoft.com/en-us/windows/win32/coreaudio/loopback-recording)
- [pywebrtc-audio: APIと10ms処理単位](https://github.com/strands-labs/pywebrtc-audio)

各参照への直列AECと入力間の相関による重複抑制は本プロジェクトの実装であり、
これらの資料や他ソフトが同じ構成を採用していることを示すものではない。
